/**
 * Категория — самостоятельный документ, а не строка, вычисленная по задачам.
 * Так её можно завести заранее (ещё без задач) и удалить насовсем: раньше
 * дефолтная четвёрка была захардкожена в экранах и возвращалась после удаления.
 */
export interface Category {
  id: string;
  name: string;
  createdAt: number;
}
